name: CI for Issue 6

on:
  push:
    paths:
      - "apps/issue-6/**"
      - ".github/workflows/ci_6.yml"
  pull_request:
    paths:
      - "apps/issue-6/**"
      - ".github/workflows/ci_6.yml"

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        working-directory: apps/issue-6
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt
          pip install -e .

      - name: Run tests
        working-directory: apps/issue-6
        run: pytest -v

      - name: Build package
        working-directory: apps/issue-6
        run: python -m build

  package:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build package
        working-directory: apps/issue-6
        run: |
          python -m pip install --upgrade pip
          pip install build
          python -m build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: issue-6-build
          path: |
            apps/issue-6/dist/
            apps/issue-6/build/
          retention-days: 90
